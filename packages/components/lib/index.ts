/**
 * Adds two numbers together.
 *
 * @param x The LHS expression
 * @param y The RHS expression
 * @returns The sum of LHS and RHS
 */
export function add(x: number, y: number): number {
  return x + y;
}
